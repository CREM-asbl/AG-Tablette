# Android wrapper

Placez ici le projet Android versionne qui publie `be.crem.ag.twa`.

## Attendu

- projet Gradle capable de produire un `.aab`
- `targetSdkVersion` / `compileSdkVersion` maintenus a jour
- signature geree par secrets CI
- commande de preparation optionnelle
- commande de publication renseignee dans `store/store.config.json`
